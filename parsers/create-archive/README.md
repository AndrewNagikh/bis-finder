# 📦 Создание архива аддона

Этот скрипт автоматически создает ZIP архив аддона BiSFinder для загрузки на CurseForge.

## 🚀 Использование

```bash
npm run create-archive
```

## 📋 Что делает скрипт

1. **Копирует файлы аддона** из папки `../addon` во временную папку
2. **Исключает ненужные файлы** (README.md, .DS_Store, node_modules и т.д.)
3. **Создает ZIP архив** с правильной структурой для CurseForge
4. **Сохраняет архив** в папку `./archives` с именем `BiSFinder.zip`
5. **Очищает старые архивы** (оставляет только последние 5)

## 📁 Структура архива

Архив содержит все необходимые файлы аддона в корне:

```
BiSFinder.zip
├── BiSFinder.lua
├── BiSFinder.toc
├── ClassButtons.lua
├── SourceMenu.lua
├── UIElements.lua
└── Sources/
    ├── SourceManager.lua
    └── IcyVeins/
        ├── IcyVeinsModule.lua
        ├── IcyVeinsData.lua
        └── ClassSpecMapping.lua
```

## 🎯 Для CurseForge

После создания архива:

1. Перейдите в папку `parsers/archives/`
2. Найдите файл `BiSFinder.zip`
3. Загрузите его на CurseForge как новый релиз

## ⚙️ Настройки

Вы можете изменить настройки в файле `archive-addon.ts`:

- `addonPath` - путь к папке аддона
- `outputDir` - папка для сохранения архивов
- `archiveName` - имя архива
- `excludePatterns` - паттерны файлов для исключения

## 🔧 Требования

- Node.js
- Системная команда `zip` или `tar` для создания архива
